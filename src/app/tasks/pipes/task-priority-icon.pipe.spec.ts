import { TaskPriorityIconPipe } from './task-priority-icon.pipe';

describe('TaskPriorityIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskPriorityIconPipe();
    expect(pipe).toBeTruthy();
  });
});
